class CustomUser {
  String name, image, uid, email, fullname, bio;
  bool isArtist, hasBio;

  CustomUser(
      {this.name,
      this.image,
      this.uid,
      this.email,
      this.bio,
      this.isArtist,
      this.hasBio});
}
